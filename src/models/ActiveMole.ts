

export class ActiveMole {
  index: number;
  startTime: number;

  constructor(index: number, startTime: number) {
    this.index = index;
    this.startTime = startTime;
  }
};

export default ActiveMole;